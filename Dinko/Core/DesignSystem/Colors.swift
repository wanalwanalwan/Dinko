import SwiftUI

enum AppColors {
    // MARK: - Backgrounds
    static let background = Color(light: "FAF8F5", dark: "121212")
    static let cardBackground = Color(light: "FFFFFF", dark: "1E1E1E")

    // MARK: - Primary
    static let teal = Color(hex: "0EA5A2")
    static let primaryTint = Color(light: "E8F6F5", dark: "1A3332")
    static let coral = Color(hex: "F07167")

    // MARK: - Semantic
    static let successGreen = Color(hex: "34C759")
    static let warningOrange = Color(hex: "F59E0B")

    // MARK: - Text
    static let textPrimary = Color(light: "1A1A2E", dark: "F0F0F0")
    static let textSecondary = Color(light: "6B7280", dark: "9CA3AF")

    // MARK: - Surfaces & Borders
    static let surfaceDark = Color(hex: "1C1C2E")
    static let separator = Color(light: "E5E7EB", dark: "2E2E2E")
    static let lockedGray = Color(light: "C7C7CC", dark: "4A4A4A")

    // MARK: - Skill Tiers
    static let tierBlue = Color(hex: "5B9BD5")
    static let tierGold = Color(hex: "F5A623")

    // MARK: - Celebration
    static let trophyGold = Color(hex: "D4A017")

    // MARK: - Drill Type Pills
    static let drillOrange = Color(hex: "F59E0B")
    static let drillPurple = Color(hex: "8B5CF6")

    // MARK: - Achievement Badges
    static let achievementPink = Color(light: "FFD6D6", dark: "4A2020")
    static let achievementBlue = Color(light: "D6EEFF", dark: "1A2E3D")
    static let achievementYellow = Color(light: "FFF3D6", dark: "3D3520")

    // MARK: - Banner
    static let bannerBackground = Color(light: "E8F6F5", dark: "1A3332")

    // MARK: - Splash Gradient
    static let splashGradientStart = Color(hex: "12BDB9")
    static let splashGradientEnd = Color(hex: "0A8E8B")

    // MARK: - Overlay
    static let overlayScrim = Color.black
}
