import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationStack {
            Text("Dinko")
                .font(.largeTitle)
                .fontWeight(.bold)
        }
    }
}

#Preview {
    ContentView()
}
